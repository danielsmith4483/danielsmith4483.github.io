import { h, Component } from "preact";
import "./style";

export default class HeroImage extends Component {
  render() {
    return (
      <div className="hero">
      test
      </div>
    );
  }
}
